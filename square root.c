#include<stdio.h>
#include<math.h>

int main(){

  int ans, num;
  printf("Enter the number \n ");
  scanf("%d", &num);
  ans = sqrt(num);
  printf("square root of the number is %d", ans);
  return 0;

}
